{
    "scripts": {
        "setup": "git submodule init && git submodule update && uv venv .venv && uv pip install -e . && echo '✓ Setup complete. Activate: source .venv/bin/activate. Configure namelist.suews in WSPS/ for your site parameters.'",
        "run": "cd WSPS && ../.venv/bin/python wsps.py && cd ../coupling-automator && make && echo 'Compilation directory ready: compilation-$(date +%Y%m%d). Next: cd ../compilation-$(date +%Y%m%d) && ./configure && ./compile em_real'",
        "archive": "rm -rf .venv && rm -rf compilation-*"
    }
}